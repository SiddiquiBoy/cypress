/**
 * @description interface to implement pagination in apis
 * @author Pulkit Bansal
 * @date 2020-04-27
 * @export
 * @interface PaginationData
 */
export interface PaginationData {
  page?: number;
  size?: number;
  // offset?: number;
}

