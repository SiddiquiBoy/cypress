import { DispatchBoardEvent } from './dispatch-board-event';

describe('DispatchBoardEvent', () => {
  it('should create an instance', () => {
    expect(new DispatchBoardEvent()).toBeTruthy();
  });
});
