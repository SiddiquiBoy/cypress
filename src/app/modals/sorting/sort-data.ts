/**
 * @description interface to implement sorting in apis
 * @author Pulkit Bansal
 * @date 2020-04-27
 * @export
 * @interface SortData
 */
export interface SortData {
    sortOrder?: string;
    sortColumn?: string;
}
