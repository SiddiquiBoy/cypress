/**
 * @description Constant values for displaying
 * UI components.
 * @author Aman Purohit
 * @date 2020-08-03
 * @export
 * @class DisplayConstant
 */

export class DisplayConstant {
    public static MAX_TAGS = 4; // Display maximum tags in job summary view
    public static MAX_ADDRESSES = 1; // Display maximum address in Job summary view
    public static MAX_SELECTION = 2; // Display maximum selection in multi-select input
    public static SHOW_HIDE_COL_MAX_SELECTION = 1; // Display maximum selection in multi-select input
    public static SCHEDULE_COL_MAX_SELECTION = 1; // Display maximum selection in multi-select input
}
